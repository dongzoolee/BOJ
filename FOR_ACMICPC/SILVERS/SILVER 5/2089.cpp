#include <iostream>
#include <vector>
#include <math.h>
using namespace std;

int main() {
	int input, temp, i = 0;
	cin >> input;
	vector<int> vecInt;
	temp = input;

	while (temp == 0) {
		temp -= (int)pow(-2, i);
		i++;
}
}