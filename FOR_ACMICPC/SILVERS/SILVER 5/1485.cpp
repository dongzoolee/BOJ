#include <iostream>
#include <vector>
using namespace std;

int main() {
	int tc, input;
	vector < vector <int> > v;
	vector <int> v2;
	v.push_back(v2);
	cin >> tc;

		for (int h = 0; h < tc; h++) {
			cin >> input;
			v[0].push_back(input);
			cin >> input;
			v[1].push_back(input);
		}



}